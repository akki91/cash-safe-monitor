/**
 * Routes: /api/config
 *
 * Placeholder — no config endpoints needed currently.
 */

import type { FastifyInstance } from "fastify";

export async function configRoutes(_app: FastifyInstance): Promise<void> {
  // No config routes needed after collateral_configs table removal
}
